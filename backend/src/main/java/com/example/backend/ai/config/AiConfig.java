package com.example.backend.ai.config;

/**
 * Spring AI tự auto-configure ChatClient.Builder qua spring-ai-openai-spring-boot-starter.
 * Không cần @Bean thêm — inject ChatClient.Builder trực tiếp vào service.
 */
public class AiConfig {
}
